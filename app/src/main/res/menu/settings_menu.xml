<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- profile/account option for all users -->
    <item
        android:id="@+id/profileMenuItem"
        android:title="Profile"
        android:icon="@android:drawable/ic_menu_myplaces"
        app:showAsAction="never" />

    <!-- logout option for all users -->
    <item
        android:id="@+id/logoutMenuItem"
        android:title="Logout"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        app:showAsAction="never" />

    <!-- settings option - admin only (will be shown/hidden dynamically) -->
    <item
        android:id="@+id/settingsMenuItem"
        android:title="Settings"
        android:icon="@android:drawable/ic_menu_preferences"
        android:visible="false"
        app:showAsAction="never" />

</menu>